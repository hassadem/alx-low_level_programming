#include "main.h"

/**
 * _isdigit - this is a function to check through digits
 * @c: function parameter
 *
 * Return: 1 when success and 0 on fail
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
