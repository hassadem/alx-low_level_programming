#include <stdio.h>
/**
 * main - this main function to run the number program 
 * Return: once the code ternimate it print 0
 */
int main(void)
{
	int i;
	
	for (i = 0 ; i < 9 ; i++)
		putchar(i);
	return (0);
}
